
            type Calculator interface {
                Add(x, y int) int;
                Subtract(a, b float, c int) [3]int;
                Reset();
                SayHello(name string);
            };
        