
        type tuananh interface {
            Add (x int, y float, c string);
            Sub (x, y int);
            Mul (x, y, z float, d string) string;
            Div () [3][2]int;
            test () hack; 
        }
        